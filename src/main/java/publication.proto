syntax = "proto3";

package org.project;

message Publication {
  int32 stationId = 1;
  string city = 2;
  int32 temp = 3;
  double rain = 4;
  int32 wind = 5;
  string direction = 6;
  string date = 7; // we'll use ISO date string (e.g. "2025-06-15")
}
